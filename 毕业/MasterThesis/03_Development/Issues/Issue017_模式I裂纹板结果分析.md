# Issue017：模式 I 裂纹板结果分析（DEM区宽度敏感性）

**模块：** 后处理（Python + ParaView）
**状态：** OPEN
**优先级：** P1
**工作量：** 2天
**论文章节：** §5.3.3-5.3.7（裂纹扩展图 + 界面行为 + 宽度敏感性分析）

---

## 任务描述

对 Issue016 完成的4个工况进行结果分析，这是论文第五章的核心内容之一。

## 需要生成的图

| 图编号 | 内容 | 对应章节 |
|--------|------|---------|
| Fig_CH5_NO4 | 裂纹扩展过程（t1/t2/t3/t4 时刻快照，代表工况W3） | §5.3 |
| Fig_CH5_NO5 | 应力强度因子KIC时程曲线（4工况对比） | §5.3 |
| Fig_CH5_NO6 | DEM区宽度 vs Ghost Force（柱状图） | §5.3 |
| Fig_CH5_NO7 | DEM区宽度 vs KIC误差（折线图） | §5.3 |
| Fig_CH5_NO8 | InterfaceBond 键力分布（VTK可视化） | §5.3 |

## KIC 提取方法

```python
# 从 Monitor Point CSV 提取裂纹尖端应力
# 使用 J积分或应力强度因子公式
import numpy as np

# 方法1（近似）：从应力场提取
sigma_y = ...  # 裂纹尖端 y 方向应力
r = 0.001      # 距裂纹尖端距离
KI = sigma_y * np.sqrt(2 * np.pi * r)

# 方法2：荷载计算
P = ...        # 总施加力
a = 0.025      # 裂缝半长
W = 0.1        # 板宽
F_factor = ...  # 几何修正因子（查表）
KIC = P / (W * t) * np.sqrt(np.pi * a) * F_factor
```

## DEM区宽度敏感性表格（论文 Table 5-x）

| DEM区宽度 | Ghost Force (%) | KIC误差 (%) | 计算时间 (s) | 推荐 |
|---------|----------------|------------|------------|------|
| 1d | — | — | — | — |
| 2d | — | — | — | — |
| 3d | — | — | ← 推荐 | ✓ |
| 5d | — | — | — | — |

目标：找到 Ghost Force < 1% 且 KIC误差 < 10% 的最小DEM区宽度（效率最优）

## 测试方法

```
通过条件：
1. 4个工况均有完整结果数据
2. 敏感性曲线单调（宽度增大→Ghost Force减小）
3. 生成5张论文级图（300 DPI）
4. Table 5-x 数据完整填写
```

## 依赖

Issue016（4个工况算例已运行完成），Issue010（InterfaceBond VTK输出用于Fig_CH5_NO8）
